services:
  app:
    container_name: teknik-stokgudang
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8082:80"
    volumes:
      - .:/app
      - /app/vendor
    networks:
      - laravel-network

networks:
  laravel-network:
    external: true